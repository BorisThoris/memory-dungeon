# GLD-P2-001 Route Profile Budget Policy

## Status
Done

## Problem
Route profile budgets were design metadata rather than executable generation policy.

## Implemented Behavior
`run-map` exposes a deterministic route budget policy and report for safe, greed, and mystery shares across generated route nodes.

## Verification
- `yarn vitest run src/shared/route-foundation.test.ts`

