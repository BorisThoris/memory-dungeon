# GLD-FB-008 - Objective Impact Matrix

Status: Done

`SAFE_EXPANSION_IMPACT_ROWS` records the objective and Perfect Memory impact for `ward_spark`, `scout_glint`, and `ward_cache`. The two shipped findables are Perfect Memory-safe; `ward_cache` remains neutral until runtime rules are versioned.
