/** In-run hint when Gambit third flip is available (visible strip + HUD polite region). */
export const GAMBIT_OPPORTUNITY_HINT_LINE =
    'Gambit: turn a third card to complete a pair, or tap a missed card to go on. It locks Perfect Memory.';

/** Keyboard shortcuts overlay (non-row tip; mirrors Gambit mismatch-resolve behavior). */
export const GAMBIT_KEYBOARD_HELP_TIP =
    'After a mismatch, Gambit lets you flip one more tile once per floor to try to complete a pair. It locks Perfect Memory.';
